# NAIP Project — Session Handoff Summary (2026-08-27)

## Project
National Agriculture Intelligence Platform (NAIP) — Pakistan agriculture intelligence
platform: satellite hazard detection, water/crop intelligence, and an audited insurance
trigger engine. Working directory: `C:\Users\USER\Desktop\NASTP\Project`. **Read
`CLAUDE.md` first in the new session** — it has the authoritative Sprint status section
(Weeks 1–23) with real numbers, caveats, and gaps. Also read
`docs/PHASE4_SCOPE_DOCUMENT.md` and `docs/PHASE5_SCOPE_DOCUMENT.md` for the track lists
each phase covers and which are closed vs. still open.

Status: Weeks 1–23 all closed (MVP through Phase 5's Track Q). This session did **not**
add a new numbered Week — it's a post-demo dashboard productization pass (UI/IA rework,
not new modeling), currently **paused mid-task awaiting your confirmation** on one
decision. See "Immediate next step" below — that's the real, only thing blocking
progress right now.

## Where this session left off — read this part first

The client demo is done. The current task ("Post-demo productization pass") is turning
the dashboard from "a demo project" into something that reads as a deliverable product:
removing demo chrome, renaming internal track/week labels in user-facing copy, and
consolidating navigation into hover dropdowns.

**A full audit was completed and reported to you; nothing has been built yet.** The
audit found:
- No actual "tip card"/demo-scaffolding language anywhere (the codebase's copy was
  already careful) — the only structural demo artifact is the Demo Walkthrough page
  itself (to be deleted entirely, confirmed, no ambiguity).
- Internal track/week labels used as literal section headings or inline prose in 9
  files (`hazards`, `water-stress`, `crop-classifier`, `exposure-risk`,
  `trigger-engine`, `models-in-production`, `crop-stress`, `demo-walkthrough`,
  `PipelineHealthBadge.tsx`) — e.g. `"National crop-share regressor (Track F,
  115-district scope)"`, `"Flood risk (Track D)"`. Plan: rename to plain client-facing
  names, keep internal codenames in an expandable technical-detail note.
- The Overview page's map hero already loads full-bleed at `58vh`, no scroll needed —
  already satisfies the "map as hero" requirement, no change needed.
- Recommended **against** anchoring the national map on every other page (Hazards,
  Water Stress etc. already have their own purpose-built maps) — reported as an
  assessment, not built.

**A proposed nav structure was presented and is awaiting your confirmation**
(this is the real, single blocking item):

| Top-level | Type | Sub-items |
|---|---|---|
| Overview | single link | — |
| Hazard Monitoring | dropdown | National Hazards (incl. Fire Detection Model), Locust Risk, Crop Stress Screen |
| Water & Climate | dropdown | Canal Water Stress (anchor), Flood Risk Screen (anchor), National Drought Signal (anchor) — all on `/water-stress` |
| Crop Intelligence | dropdown | Irrigation Classifier, National Crop Model, Cross-Year Validation, Yield Prediction (anchors, all on `/crop-classifier`) |
| Insurance Engine | dropdown | Exposure Risk, Trigger Engine |
| AI Models | single link | Models-in-Production (kept standalone, not folded in — avoids duplicating model stats) |

Anchors (not new routes) were chosen deliberately for the Water Stress/Crop Intelligence
sub-items — keeps a one-scroll path to every caveat, touches zero data-fetching logic.

**Two smaller judgment calls flagged, also awaiting confirmation**:
1. `trigger-engine/page.tsx`'s `basis_risk_note` is explicitly labeled "verbatim from
   the audit record" and is real Python-generated text (from
   `trigger_engine.py`'s `BASIS_RISK_NOTE` constant) containing phrases like "PHASE 3
   WEEK 9 (Track G)." Proposed: leave this field's wording untouched (don't break its
   own "verbatim" claim), add a one-line client-side gloss above it instead. Could
   instead touch the Python source if you'd rather the underlying audit text itself
   read cleaner.
2. `PipelineHealthBadge.tsx`'s toggle button is literally labeled `"real limitation"`.
   Proposed: rename to `"details"`, retitle "Live nowcasting loop (Track H)" → "Live
   Data Pipeline" (Track H kept in the expandable detail).

**Immediate next step for the new session**: get the user's confirmation on the nav
table above (and the two smaller calls), then execute the full pass — remove Demo
Walkthrough page/route, rename track/week labels across the 9 files, build the hover
dropdown nav (real keyboard/touch accessibility, no hover-only trap on mobile — check
this specifically), verify every honesty caveat is still present at full visibility
after the restructure (basis risk notes, three-tier crop-mix labeling, "not a flood
alert," "not a pest/disease diagnosis," the sugarcane/yield negative results, model-vs-
baseline comparisons — none may be shrunk or removed, per the non-negotiable stated
first in the original task), verify live in the browser (type-check clean, zero console
errors, real data still rendering, nothing broken by the nav restructure), then report
the real result and update `CLAUDE.md` if this becomes a numbered week.

## Track H's live loop — genuinely still running right now

Confirmed via `Get-ScheduledTask`/`Get-ScheduledTaskInfo` moments before this handoff:
`State: Running`, real cumulative state (`naip/data/live_nowcast/cycle_state.json`):
**110 success / 12 failure / 8 no-new-scene** since Week 13, last real success
`2026-08-27T13:34:02Z`. `district_alerts.json` keeps growing additively with each real
cycle — historical rows have stayed untouched through every failure, confirmed
repeatedly across many prior sessions.

**Do NOT stop or restart this loop without checking `naip/logs/live_nowcast.log` and
`cycle_state.json` first** — it's genuinely live in production. The productization pass
above doesn't touch it at all; no reason to pause it for UI/copy work.

## Two real GitHub repos — real current state

1. `msd-arch/MSG-SEVIRI-Product-Dashboard` — original pilot dashboard, untouched,
   unrelated.
2. `msd-arch/naip-platform` (main codebase, local: `C:\Users\USER\Desktop\NASTP\Project\naip`)
   — last commit `b15bd7b` (Track Q), pushed. **232 files currently uncommitted** —
   this is routine: Track H's live loop keeps writing new `data/live_nowcast/web_data/`
   grid files and resyncing `district_alerts.json`/`cycle_state.json` every ~15min: not
   new work to review, just accumulated live-loop output. Safe to commit+push whenever
   convenient (same credential-scan discipline as every prior commit this project).
3. `msd-arch/NAIP` (dashboard, local: `C:\Users\USER\Downloads\naip_dashboard`) — last
   commit `db92a04` (Track Q dashboard page), pushed. **3 files uncommitted** — same
   routine live-data resync (`district_alerts.json`, `district_hazard_summary.json`,
   `pipeline_health.json`), not code. The productization pass has not modified any
   dashboard code yet — no in-progress work sitting uncommitted.

## Known real gaps / standing decisions carried forward

- Flood model (Track D/I): original 2022-only model stays deployed; v2 rejected.
  Neither wired into `exposure_risk.py`/`trigger_engine.py`. Live screen stays
  informational-only.
- `model_estimated_interim` tier: wired into `exposure_risk.py`/`trigger_engine.py` as
  of Week 20, with a real per-crop confidence discount added Week 21 (wheat needs
  ~2.1x raw score to trigger, sugarcane ~8.2x). Illustrative threshold (0.225) stays at
  0 real events; demo threshold re-derived to 0.0216 (9 real events, 6 cotton/3 rice).
- Track O (yield prediction, Week 22): real, honest **negative** result — a naive
  year-over-year persistence baseline beats the trained model in 6/8 crop×direction
  combinations. Reported as a real useful-but-limited input, not a solved problem.
- Track Q (pest/disease screening, Week 23): real pre-check found no extractable
  per-location outbreak data for Pakistan (RustTracker.org dead, GRRC map-only, closest
  real survey only publishes district aggregates) — fallback "crop stress early-warning
  screen" built instead, explicitly non-diagnostic in its own UI text, not just docs.
- Docker/PostGIS and Twilio: both real, both open, both explicitly assessed as
  not-worth-forcing this phase (checked directly each time revisited, not assumed
  stale).
- No Phase 6 scope document exists yet — Phase 5's Track P (minimal farmer data
  collection mechanism) is the last named-but-not-yet-started track in
  `PHASE5_SCOPE_DOCUMENT.md`.

## Immediate next steps (pick up here)

1. **Confirm the nav structure and the two smaller flagged decisions above** — this is
   the only real blocker. Don't re-derive the proposal from scratch; it's already been
   built from a real, complete audit of every page's actual content.
2. Execute the productization pass once confirmed (Parts 1–4 as scoped in the original
   task prompt), verify live in the browser, report the real result.
3. Commit + push both repos' accumulated live-loop data (routine, low-risk) whenever
   convenient — either alongside the productization pass commit or separately.
4. Longer-term open items, not urgent: Track P (farmer data collection, real stakes
   around fabrication since it touches identity data), a Phase 6 scope doc if the
   project continues past Phase 5, and the standing Docker/Twilio decisions above if
   priorities change.
